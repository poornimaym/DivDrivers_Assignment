#include<stdio.h>
int main()
{
	/*comment1 /*comment2 */*/
	return 0;
}

/* once the comment(/*) is started the compiler look for nearest ending '*/' this leads to uncommenting of somw of the comments which might leads to unnecessary trouble/Error so Line by line Commenting is good. */
